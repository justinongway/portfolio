name: Deploy to GitHub Pages

on:
  push:
    branches:
      - main

jobs:
  deploy:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout repository
        uses: actions/checkout@v6

      - name: Deploy to GitHub Pages
        uses: portfolio/justinongway/release@v6
        with:
          folder: .  # This indicates t
